---
phase: 08-admin-and-disputes-e2e
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - e2e/admin-actions.spec.ts
autonomous: true
requirements:
  - E2E-07
  - E2E-08
  - E2E-09
  - E2E-10
user_setup: []

must_haves:
  truths:
    - "e2e/admin-actions.spec.ts imports test and expect from './fixtures' (not '@playwright/test')"
    - "Review moderation tests use the adminPage fixture for admin actions"
    - "Review approve test: admin navigates to /admin/reviews, expands a review card, clicks 'Reset to Pending', then clicks 'Approve', and the status badge changes to 'approved'"
    - "Review reject test: admin navigates to /admin/reviews, expands a review card, clicks 'Reset to Pending', then clicks 'Reject', and the status badge changes to 'rejected'"
    - "Dispute submission test uses unauthenticated page fixture (page, not adminPage)"
    - "Dispute submission fills reviewUrl with http://localhost:8788/building/12-brighton-ave#review-review-001"
    - "Dispute submission fills landlordName, landlordEmail, landlordPhone"
    - "Dispute submission checks at least one dispute reason checkbox"
    - "Dispute submission asserts 'Dispute submitted successfully' becomes visible"
    - "Dispute resolution test: admin expands a pending dispute, fills resolution notes, clicks 'Resolve Dispute'"
    - "Dispute resolution asserts the dispute status changes to 'resolved'"
    - "Audit log test navigates to /admin/audit and asserts at least one table row exists"
    - "Audit log test verifies table has columns: Timestamp, Admin, Action, Entity, Details"
    - "Audit log row expansion shows 'From:' label for old/new value details"
    - "All tests verify UI reflects the change (status badges update, audit entries appear)"
  artifacts:
    - path: "e2e/admin-actions.spec.ts"
      provides: "E2E specs for review moderation, dispute submission, dispute resolution, and audit log verification"
      contains: "Resolve Dispute"
      min_lines: 100
  key_links:
    - from: "e2e/admin-actions.spec.ts"
      to: "e2e/fixtures.ts"
      via: "import { test, expect } from './fixtures'"
      pattern: "from './fixtures'"
    - from: "e2e/admin-actions.spec.ts"
      to: "src/pages/api/admin/reviews/[id].ts"
      via: "PATCH /api/admin/reviews/:id triggered by UI button click"
      pattern: "Approve|Reject"
    - from: "e2e/admin-actions.spec.ts"
      to: "src/pages/api/disputes.ts"
      via: "POST /api/disputes triggered by dispute form submission"
      pattern: "Dispute submitted successfully"
    - from: "e2e/admin-actions.spec.ts"
      to: "src/pages/api/disputes/[id].ts"
      via: "PATCH /api/disputes/:id triggered by resolve button click"
      pattern: "Resolve Dispute"
    - from: "e2e/admin-actions.spec.ts"
      to: "src/lib/audit.ts"
      via: "createAuditLog called by review/dispute PATCH APIs, verified via /admin/audit UI"
      pattern: "audit"
---

<objective>
Create e2e/admin-actions.spec.ts covering E2E-07 (review moderation), E2E-08 (dispute submission), E2E-09 (dispute resolution), and E2E-10 (audit log entries). Tests admin actions through the real UI, verifying that status changes propagate and audit entries are created.

Purpose: After this plan, every admin moderation action, the public dispute submission flow, and the audit log have automated E2E coverage. Combined with Plan 01, this completes all Phase 8 requirements.
Output: 1 new spec file (e2e/admin-actions.spec.ts) with ~8-10 test cases.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@e2e/fixtures.ts
@playwright.config.ts
@src/components/admin/ReviewsTable.tsx
@src/components/admin/DisputesQueue.tsx
@src/components/admin/AuditLogTable.tsx
@src/components/disputes/DisputeForm.tsx
@src/pages/api/admin/reviews/[id].ts
@src/pages/api/disputes.ts
@src/pages/api/disputes/[id].ts
@src/pages/api/admin/audit.ts

# Plan 01 SUMMARY needed — admin-pages.spec.ts must exist first
@.planning/phases/08-admin-and-disputes-e2e/08-01-SUMMARY.md

<interfaces>
<!-- Existing fixtures (from Phase 6) -->
```typescript
// e2e/fixtures.ts exports:
export const test = base.extend<CustomFixtures>({
  authedPage: async ({ browser }, use) => { /* user.json storageState */ },
  adminPage: async ({ browser }, use) => { /* admin.json storageState */ },
});
export { expect };
```

<!-- ReviewsTable.tsx — Review card structure and action buttons -->
Each review card:
- Header: clickable div.cursor-pointer with building address, user email, score, status badge
- Click header to expand/collapse
- Expanded view shows action buttons:
  - "Approve" button (bg-green-600) — only shown when status !== 'approved'
  - "Reject" button (bg-red-600) — only shown when status !== 'rejected'
  - "Reset to Pending" button — only shown when status !== 'pending'
- Status badge: span.rounded-full with status text (pending/approved/rejected/flagged)
- Buttons use e.stopPropagation() — clicking them won't collapse the card
- PATCH /api/admin/reviews/{id} with { status: newStatus } on button click

CRITICAL: All seed reviews start as 'approved'. To test approve/reject:
1. Expand a review card
2. Click "Reset to Pending" first — sets status to pending
3. Wait for status badge to show "pending"
4. Now "Approve" or "Reject" button appears — click it
5. Verify status badge updates

<!-- DisputeForm.tsx — Public dispute form fields -->
Form field IDs:
- #reviewUrl — Review URL input
- #landlordName — Landlord name input
- #landlordEmail — Landlord email input
- #landlordPhone — Landlord phone input
- Checkboxes: use page.getByLabel('Factually incorrect information').check() — avoids CSS ID escaping issues with spaces in IDs
- #disputeExplanation — Additional details textarea (optional)
- button[type="submit"] — Submit button

Success state: green container with h3 "Dispute submitted successfully"
Validation errors: p.text-sm.text-red-600 with field-specific messages

CRITICAL: Review URL must use localhost:8788 origin. extractReviewIdFromUrl() validates origin matches.
Valid URL: http://localhost:8788/building/12-brighton-ave#review-review-001
- review-001 is on building-01 (slug: 12-brighton-ave)
- No existing dispute for review-001 in seed data (avoids UNIQUE constraint 409)

<!-- DisputesQueue.tsx — Dispute resolution form -->
- Default filter: 'pending' — 7 seed pending disputes visible
- Click card header (div.cursor-pointer) to expand
- Expanded view shows side-by-side:
  - h4 "Landlord Information", h4 "Original Review", h4 "Resolve Dispute"
- Resolution form:
  - Outcome select dropdown (defaults to 'dismiss')
  - Notes textarea: placeholder "Explain the resolution decision..." — REQUIRED (button disabled when empty)
  - "Resolve Dispute" button
- After resolution: card collapses, dispute disappears from pending filter
- Switch to "Resolved" or "All" filter to verify resolved status badge

<!-- AuditLogTable.tsx — Audit log display -->
- Table columns: Timestamp, Admin, Action, Entity, Details
- Action badge: formatActionType() converts 'review_approved' -> 'Review Approved'
- Rows clickable to expand — shows "From:" and "To:" labels for old/new values
- Empty state: "No audit logs found."
- Seed data has NO audit_log rows — entries created only by E2E admin actions
- workers: 1 guarantees moderation tests run before audit log test

<!-- Seed data reference -->
- Reviews: 100+ reviews, ALL status 'approved' — must "Reset to Pending" first
- review-001: on building-01 (slug 12-brighton-ave), by user-test-01
- Disputes: 7 pending (dispute-01..07), 3 resolved (dispute-08..10)
- dispute-01: review_id=review-005, NOT review-001 — safe to submit dispute for review-001
- Audit logs: NONE seeded — table starts empty

<!-- Key patterns -->
- Import from './fixtures' not '@playwright/test'
- workers: 1 (sequential execution)
- baseURL: http://localhost:8788
- test.setTimeout(60000) for longer sequences
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create e2e/admin-actions.spec.ts with moderation, dispute, and audit tests</name>
  <files>e2e/admin-actions.spec.ts</files>
  <action>
Create a new file `e2e/admin-actions.spec.ts` with the following test structure. This file covers E2E-07 (review moderation), E2E-08 (dispute submission), E2E-09 (dispute resolution), and E2E-10 (audit log entries).

The file must:

1. **Import from './fixtures'** (not '@playwright/test') — matching the project convention
2. **Use the `adminPage` fixture** for admin action tests
3. **Use the `page` fixture** (unauthenticated) for dispute submission (public form)
4. **Set appropriate timeouts** via `test.setTimeout(60000)` for tests with multiple UI actions

**describe('Review Moderation (E2E-07)', () => { ... })**

All seed reviews are 'approved'. The strategy for each moderation test:
1. Navigate to /admin/reviews, wait for networkidle
2. Expand a review card (click .cursor-pointer div)
3. Click "Reset to Pending" to make the Approve/Reject button appear
4. Wait for status badge to show "pending"
5. Click the target action button
6. Assert status badge updates

- **'admin can approve a pending review'**:
  - Use `adminPage`, set timeout 60000
  - Navigate to `/admin/reviews`, wait for networkidle
  - Click first `.cursor-pointer` to expand first review card
  - Click `button` with text "Reset to Pending"
  - Wait: `expect(adminPage.locator('span.rounded-full').first()).toContainText('pending')`
  - Click `button` with text "Approve"
  - Assert: `expect(adminPage.locator('span.rounded-full').first()).toContainText('approved')`

- **'admin can reject a pending review'**:
  - Use `adminPage`, set timeout 60000
  - Navigate to `/admin/reviews`, wait for networkidle
  - Click "Approved" filter button to view approved reviews
  - Click second `.cursor-pointer` to expand a different review
  - Click "Reset to Pending", wait for "pending" badge
  - Click `button` with text "Reject"
  - Assert: status badge shows "rejected"

**describe('Dispute Submission (E2E-08)', () => { ... })**

The /dispute page is public. Use unauthenticated `page` fixture.

- **'landlord can submit a dispute through the public form'**:
  - Use `page` fixture
  - Navigate to `/dispute`
  - Wait for React hydration: `await page.locator('#reviewUrl').waitFor()`
  - Fill form:
    - `page.fill('#reviewUrl', 'http://localhost:8788/building/12-brighton-ave#review-review-001')`
    - `page.fill('#landlordName', 'E2E Test Landlord')`
    - `page.fill('#landlordEmail', 'e2e-test-landlord@example.com')`
    - `page.fill('#landlordPhone', '617-555-9999')`
  - Check reason: `page.getByLabel('Factually incorrect information').check()` (avoids CSS ID space escaping)
  - Fill explanation: `page.fill('#disputeExplanation', 'This is an E2E test dispute submission.')`
  - Click submit: `page.locator('button[type="submit"]').click()`
  - Assert: `expect(page.locator('text=Dispute submitted successfully')).toBeVisible()`

- **'dispute form validates required fields'**:
  - Use `page` fixture
  - Navigate to `/dispute`, wait for React hydration
  - Click submit without filling fields
  - Assert validation error visible: `expect(page.locator('text=Review URL is required')).toBeVisible()` or similar field error
  - Assert success NOT visible: `expect(page.locator('text=Dispute submitted successfully')).not.toBeVisible()`

**describe('Dispute Resolution (E2E-09)', () => { ... })**

Seed data has 7 pending disputes. Use one for the resolution test.

- **'admin can view dispute side-by-side with review and resolve it'**:
  - Use `adminPage`, set timeout 60000
  - Navigate to `/admin/disputes`, wait for networkidle
  - Default filter is 'pending' — pending disputes visible
  - Click first `.cursor-pointer` to expand first dispute
  - Assert side-by-side view sections:
    - `expect(adminPage.locator('h4', { hasText: 'Landlord Information' })).toBeVisible()`
    - `expect(adminPage.locator('h4', { hasText: 'Original Review' })).toBeVisible()`
    - `expect(adminPage.locator('h4', { hasText: 'Resolve Dispute' })).toBeVisible()`
  - Fill resolution:
    - Notes: `adminPage.locator('textarea[placeholder="Explain the resolution decision..."]').fill('E2E test resolution - dispute dismissed after review.')`
    - Outcome dropdown defaults to 'dismiss' — no change needed
  - Click: `adminPage.locator('button', { hasText: 'Resolve Dispute' }).click()`
  - After resolution, card collapses and dispute leaves pending filter
  - Switch to "Resolved" filter: `adminPage.locator('button', { hasText: /Resolved/ }).click()`
  - Assert: `expect(adminPage.locator('span.rounded-full', { hasText: 'resolved' }).first()).toBeVisible()`

**describe('Audit Log (E2E-10)', () => { ... })**

Admin actions from review moderation and dispute resolution create audit log entries. Since workers=1, those tests run before these.

- **'admin actions appear in the audit log'**:
  - Use `adminPage` fixture
  - Navigate to `/admin/audit`, wait for networkidle
  - Assert table structure:
    - `expect(adminPage.locator('table thead')).toBeVisible()`
    - `expect(adminPage.locator('th', { hasText: 'Timestamp' })).toBeVisible()`
    - `expect(adminPage.locator('th', { hasText: 'Action' })).toBeVisible()`
    - `expect(adminPage.locator('th', { hasText: 'Entity' })).toBeVisible()`
  - Assert at least one row: `expect(adminPage.locator('table tbody tr').first()).toBeVisible()`
  - Assert an action badge is visible in the first row (contains text like "Review" or "Dispute")

- **'audit log row expansion shows old/new values'**:
  - Use `adminPage` fixture
  - Navigate to `/admin/audit`, wait for networkidle
  - Wait for first row: `adminPage.locator('table tbody tr').first().waitFor()`
  - Click first row to expand: `adminPage.locator('table tbody tr').first().click()`
  - Assert expanded details: `expect(adminPage.locator('text=From:').first()).toBeVisible()`

**IMPORTANT implementation notes:**

1. **React component loading**: Use `waitForLoadState('networkidle')` after navigation to all admin pages
2. **Review card expansion**: Click `.cursor-pointer` div to expand. Action buttons have `e.stopPropagation()`.
3. **Dispute checkbox IDs have spaces**: Use `page.getByLabel('Factually incorrect information')` not CSS ID selector
4. **Dispute URL origin must match**: Must be `http://localhost:8788/...` — no HTTPS
5. **Resolve button disabled without notes**: Fill textarea before clicking "Resolve Dispute"
6. **Dispute card collapses after resolution**: Switch filter to "Resolved" to verify
7. **Audit log starts empty**: Entries created by prior moderation/resolution tests. Workers=1 guarantees order.
8. **Do NOT use waitForTimeout()**: Use `waitForLoadState` or element-specific `waitFor()`.
  </action>
  <verify>node -e "const fs = require('fs'); if (!fs.existsSync('e2e/admin-actions.spec.ts')) { console.error('FAIL: e2e/admin-actions.spec.ts not found'); process.exit(1); } const src = fs.readFileSync('e2e/admin-actions.spec.ts', 'utf8'); const checks = [['./fixtures import', /from\s+['\"]\.\/fixtures['\"]/], ['adminPage fixture', /adminPage/], ['approve test', /[Aa]pprove/], ['reject test', /[Rr]eject/], ['dispute submission', /dispute.*submit|Submit.*Dispute|Dispute submitted/i], ['dispute form URL', /reviewUrl|review.*URL/i], ['dispute landlord name', /landlordName|landlord.*name/i], ['dispute resolution', /[Rr]esolve.*[Dd]ispute/], ['resolution notes', /resolution.*notes|resolutionNotes|resolution decision/i], ['audit log', /[Aa]udit.*[Ll]og|admin\/audit/], ['audit table', /Timestamp|Action|Entity/], ['E2E test data', /E2E.*[Tt]est|e2e.*test/]]; let failed = false; for (const [name, pattern] of checks) { if (!pattern.test(src)) { console.error('FAIL: missing ' + name); failed = true; } } if (src.includes(\"from '@playwright/test'\")) { console.error('FAIL: imports from @playwright/test instead of ./fixtures'); failed = true; } if (failed) process.exit(1); console.log('OK: admin-actions.spec.ts verified');"</verify>
  <done>e2e/admin-actions.spec.ts created with review moderation (approve/reject), dispute submission/validation, dispute resolution, and audit log verification tests.</done>
</task>

<task type="auto">
  <name>Task 2: Validate admin-actions spec syntax</name>
  <files></files>
  <action>
Run a syntax validation on the new spec file:

```bash
npx playwright test --list e2e/admin-actions.spec.ts 2>&1 || true
```

This should list all test names without actually running them. If it shows test names, the file is valid.

If there are errors, fix them in e2e/admin-actions.spec.ts. Common issues:
- Missing type annotations
- Import path issues (./fixtures must resolve)
- Fixture destructuring for adminPage, authedPage, page

NOTE: Validation only. Full E2E run in Task 3.
  </action>
  <verify>node -e "const fs = require('fs'); const src = fs.readFileSync('e2e/admin-actions.spec.ts', 'utf8'); if (src.includes('import { test, expect }') && (src.includes('test.describe') || src.includes('test('))) { console.log('OK: admin-actions.spec.ts has valid test structure'); } else { console.error('FAIL: missing test structure'); process.exit(1); }"</verify>
  <done>e2e/admin-actions.spec.ts passes syntax validation and Playwright can list its test cases</done>
</task>

<task type="auto">
  <name>Task 3: Run full E2E suite to verify all tests pass</name>
  <files></files>
  <action>
Run the complete E2E test suite to verify all new admin tests pass alongside existing tests:

```bash
npm run e2e 2>&1
```

This runs the full pipeline: db:setup (fresh + seed), build, then playwright test. It includes all existing tests (navigation, pages, auth, review specs) plus the new admin-pages and admin-actions specs.

**Expected outcome:** All tests pass. The new admin tests should:
- admin-pages.spec.ts: ~12-14 tests passing (all 9 pages render, navigation, access control)
- admin-actions.spec.ts: ~8-10 tests passing (approve, reject, dispute submit, dispute validate, dispute resolve, audit log entries, audit log expansion)

**If any tests fail:**

1. Read the error output carefully
2. Common issues:
   - **Timeout on React component loading**: Increase timeout or add `waitForLoadState('networkidle')` after goto
   - **Element not found**: Check selectors against actual rendered HTML
   - **Status badge not updating**: Wait for React state re-render with specific element assertion
   - **Dispute checkbox selector**: Use `page.getByLabel('...')` not CSS `#reason-...` with spaces
   - **Audit log empty**: Verify moderation tests ran first (workers: 1 guarantees file ordering alphabetically: admin-actions before admin-pages? Check and adjust if needed)
   - **Review card not expanding**: Target `.cursor-pointer` div more specifically
3. Fix issues in the spec files and re-run

The total test count should be >= 60 (50 from Phase 7 + new admin tests).

NOTE on test ordering: Playwright with workers: 1 runs test files in discovery order. admin-actions.spec.ts comes alphabetically before admin-pages.spec.ts. The audit log tests depend on moderation actions having created entries. Since the moderation tests and audit tests are in the SAME file (admin-actions.spec.ts), ordering within the file is guaranteed. If there are cross-file ordering issues, either:
1. Rename files to control order (e.g., prefix with numbers)
2. Make the audit log test self-contained by performing a moderation action within it before checking the log
  </action>
  <verify>node -e "const { execSync } = require('child_process'); try { const out = execSync('npx playwright test --list e2e/admin-pages.spec.ts e2e/admin-actions.spec.ts 2>&1', { encoding: 'utf8', cwd: process.cwd() }); const lines = out.split('\\n').filter(l => l.includes('admin') || l.includes('Admin') || l.includes('dispute') || l.includes('Dispute') || l.includes('audit') || l.includes('Audit')); console.log('Admin test files listed:', lines.length > 0 ? 'OK' : 'FAIL'); if (lines.length === 0) process.exit(1); } catch(e) { console.error('FAIL: could not list tests'); process.exit(1); }"</verify>
  <done>Full E2E suite passes including all new admin page and admin action tests. Total test count verified at >= 60.</done>
</task>

</tasks>

<verification>
- [ ] e2e/admin-actions.spec.ts exists
- [ ] Imports from './fixtures' (not '@playwright/test')
- [ ] Review approve test: admin expands review, resets to pending, then approves
- [ ] Review reject test: admin expands a different review, resets to pending, then rejects
- [ ] Status badges update in the UI after approve/reject actions
- [ ] Dispute submission fills reviewUrl, landlordName, landlordEmail, landlordPhone
- [ ] Dispute submission uses valid seed review URL (review-001 on building-01, localhost:8788)
- [ ] Dispute submission checks a reason checkbox via getByLabel
- [ ] Dispute submission asserts "Dispute submitted successfully" visible
- [ ] Dispute form validation test: submitting empty form shows errors
- [ ] Dispute resolution: admin expands pending dispute, fills notes, clicks resolve
- [ ] Dispute resolution asserts status changes to "resolved" via filter switch
- [ ] Audit log: navigates to /admin/audit and verifies table and entries exist
- [ ] Audit log shows column headers (Timestamp, Action, Entity)
- [ ] Audit log row expansion shows "From:" label
- [ ] No imports from '@playwright/test'
- [ ] Full E2E suite passes (all tests including existing Phase 6/7 tests)
</verification>

<success_criteria>
- e2e/admin-actions.spec.ts contains ~8-10 well-structured test cases
- Review moderation tested: approve and reject flows verified through UI
- Dispute submission tested: public form fills all fields and submits successfully
- Dispute form validation tested: empty submission shows errors
- Dispute resolution tested: admin resolves a pending dispute with outcome and notes
- Audit log tested: entries appear after admin actions, details expandable
- File follows project conventions (./fixtures import, adminPage fixture)
- E2E-07, E2E-08, E2E-09, E2E-10 requirements fully addressed
- Full E2E suite passes with >= 60 total tests
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-and-disputes-e2e/08-02-SUMMARY.md`
</output>
