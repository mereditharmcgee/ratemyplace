---
phase: 11-schema-survey-fields-and-contact-form
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0020_contact_messages.sql
  - migrations/0021_reserved.sql
  - migrations/0022_reserved.sql
  - src/components/contact/ContactForm.tsx
  - src/pages/contact.astro
  - src/pages/api/contact.ts
  - src/lib/email.ts
  - src/pages/admin/contact.astro
  - src/pages/api/admin/contact-messages.ts
  - src/components/admin/AdminLayout.astro
autonomous: true
requirements: [CONTACT-01, CONTACT-02, CONTACT-03, CONTACT-04]

must_haves:
  truths:
    - "Contact page shows a working form with name, email, category dropdown, and message body"
    - "Form submission stores the message in D1 contact_messages table"
    - "Submitter receives a confirmation email from noreply@ratemyplace.org"
    - "Admin panel has a Contact tab showing all submissions"
    - "Contact form rejects submissions without valid Turnstile token"
    - "Contact form rate-limits to 3 submissions per hour per IP"
  artifacts:
    - path: "migrations/0020_contact_messages.sql"
      provides: "CREATE TABLE contact_messages with indexes"
      contains: "CREATE TABLE contact_messages"
    - path: "src/pages/api/contact.ts"
      provides: "POST handler with Turnstile, rate limit, D1 insert, Resend emails"
      exports: ["POST"]
    - path: "src/components/contact/ContactForm.tsx"
      provides: "React island contact form with Turnstile widget"
      exports: ["ContactForm"]
    - path: "src/pages/admin/contact.astro"
      provides: "Admin contact messages page with auth gate"
      contains: "isAdmin"
    - path: "src/lib/email.ts"
      provides: "sendContactConfirmationEmail and sendContactNotificationEmail functions"
      contains: "sendContactConfirmationEmail"
  key_links:
    - from: "src/components/contact/ContactForm.tsx"
      to: "/api/contact"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/contact"
    - from: "src/pages/api/contact.ts"
      to: "src/lib/email.ts"
      via: "sendContactConfirmationEmail call"
      pattern: "sendContactConfirmationEmail"
    - from: "src/pages/api/contact.ts"
      to: "D1 contact_messages"
      via: "INSERT INTO contact_messages"
      pattern: "INSERT INTO contact_messages"
    - from: "src/pages/admin/contact.astro"
      to: "/api/admin/contact-messages"
      via: "data fetch for table"
      pattern: "contact-messages"
---

<objective>
Build a working contact form page with D1 storage, Turnstile spam protection, rate limiting, Resend email confirmations, and an admin panel tab for viewing submissions. Also create placeholder migrations 0021-0022 to reserve those slots for the milestone.

Purpose: Replace the static mailto links with a tracked, searchable contact system that notifies admins and confirms receipt to users.
Output: Contact form page, API route, admin tab, email functions, and 3 migration files (0020-0022).
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-schema-survey-fields-and-contact-form/11-CONTEXT.md
@.planning/phases/11-schema-survey-fields-and-contact-form/11-RESEARCH.md
@.planning/phases/11-schema-survey-fields-and-contact-form/11-VALIDATION.md

<interfaces>
<!-- Key patterns the executor needs — extracted from codebase -->

From src/pages/api/bug-reports.ts (the direct analogue for contact form API):
- Uses: verifyTurnstile(), getClientIP(), checkRateLimit(), getDB(), generateIdFromEntropySize()
- Pattern: parse formData -> Turnstile verify -> rate limit check -> validate -> D1 insert -> return JSON

From src/lib/email.ts:
```typescript
export interface EmailResult {
  success: boolean;
  error?: string;
  messageId?: string;
}
// Existing functions: sendVerificationEmail, sendPasswordResetEmail, sendDisputeConfirmationEmail, sendDisputeUpheldEmail
```

From src/lib/turnstile.ts:
```typescript
export async function verifyTurnstile(token: string, secretKey: string, ip: string): Promise<{ success: boolean; error?: string }>;
```

From src/lib/rateLimit.ts:
```typescript
export async function checkRateLimit(db: D1Database, ip: string, endpoint: string, maxRequests: number, windowSeconds: number): Promise<{ allowed: boolean }>;
export function getClientIP(context: APIContext): string;
```

AdminLayout currentPage union (line 6):
```typescript
currentPage: 'dashboard' | 'users' | 'reviews' | 'buildings' | 'landlords' | 'managers' | 'verify' | 'disputes' | 'bugs' | 'audit';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration, contact API route, and email functions</name>
  <files>migrations/0020_contact_messages.sql, migrations/0021_reserved.sql, migrations/0022_reserved.sql, src/pages/api/contact.ts, src/lib/email.ts</files>
  <action>
1. Create `migrations/0020_contact_messages.sql`:
   ```sql
   CREATE TABLE contact_messages (
     id TEXT PRIMARY KEY,
     name TEXT NOT NULL,
     email TEXT NOT NULL,
     category TEXT NOT NULL DEFAULT 'general',
     message TEXT NOT NULL,
     status TEXT NOT NULL DEFAULT 'new',
     created_at INTEGER NOT NULL DEFAULT (unixepoch()),
     resolved_at INTEGER,
     admin_notes TEXT
   );
   CREATE INDEX idx_contact_messages_status ON contact_messages(status);
   CREATE INDEX idx_contact_messages_created ON contact_messages(created_at DESC);
   ```

2. Create `migrations/0021_reserved.sql` with `SELECT 1;` (placeholder to hold slot)

3. Create `migrations/0022_reserved.sql` with `SELECT 1;` (placeholder to hold slot)

4. Apply migrations locally: `npx wrangler d1 migrations apply ratemyplace-db --local`

5. Add to `src/lib/email.ts`:
   - `sendContactConfirmationEmail(apiKey, toEmail, toName, category)` — sends confirmation to submitter from `noreply@ratemyplace.org`. Follow exact pattern of existing email functions (Resend import, EmailResult return type, try/catch with console.error). Subject: "We received your message - RateMyPlace Boston". Body: greeting with name, acknowledgment of category inquiry, 2-3 business day follow-up note. Use teal heading color (#0d9488) to match brand.
   - `sendContactNotificationEmail(apiKey, submitterName, submitterEmail, category, messagePreview)` — sends notification to `contact@ratemyplace.org` so admin is alerted. Subject: `New contact: ${category} from ${submitterName}`. Body: submitter info, category, first 200 chars of message. This ensures admin doesn't have to actively check the panel.

6. Create `src/pages/api/contact.ts` with POST handler following the bug-reports.ts pattern exactly:
   - Parse formData (name, email, category, message, cf-turnstile-response)
   - Turnstile verification via `verifyTurnstile()` — return 400 on failure
   - Rate limit via `checkRateLimit(db, ip, 'contact', 3, 3600)` — return 429 on failure
   - Validate inputs: name required (2-100 chars), email required (must contain @), message required (10-3000 chars), category must be in ['general', 'privacy', 'support', 'landlord'] (default to 'general' if invalid)
   - Generate ID via `generateIdFromEntropySize(10)`
   - INSERT INTO contact_messages (id, name, email, category, message)
   - Send confirmation email to submitter (best-effort — don't fail request if email fails)
   - Send notification email to contact@ratemyplace.org (best-effort)
   - Return 200 with `{ success: true }`
  </action>
  <verify>
    <automated>npx wrangler d1 migrations apply ratemyplace-db --local 2>&1 | grep -E "Applied|already|error" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Migration 0020 creates contact_messages table. Migrations 0021-0022 are placeholders. API route handles POST with Turnstile + rate limit + D1 insert + emails. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Contact form page, admin contact tab, and admin API</name>
  <files>src/components/contact/ContactForm.tsx, src/pages/contact.astro, src/pages/admin/contact.astro, src/pages/api/admin/contact-messages.ts, src/components/admin/AdminLayout.astro</files>
  <action>
1. **ContactForm.tsx** — Create React island component at `src/components/contact/ContactForm.tsx`:
   - Fields: name (text input), email (email input), category (select dropdown with General/Privacy/Support/Landlord), message (textarea)
   - Turnstile widget: Check how `src/pages/bug-report.astro` includes the Turnstile script and widget div. Mirror that pattern. The sitekey should be passed as a prop from the Astro page or hardcoded if that's the existing pattern.
   - Client-side validation: name required, email required with @ check, message required (min 10 chars), show inline error messages
   - Submit handler: POST to `/api/contact` with FormData, show success message on 200, show error message on 4xx/429
   - Success state: Replace form with "Thank you! We've received your message and sent a confirmation to your email." message
   - Loading state: Disable submit button during submission
   - Style with Tailwind: consistent with existing form styling (see ReviewForm.tsx for input classes)

2. **contact.astro** — Replace the existing static content with the ContactForm island:
   - Keep the page layout (BaseLayout or similar)
   - Add `<ContactForm client:load />`
   - Include the Turnstile script tag in the head (check bug-report.astro for the exact script URL)
   - Add brief intro text above the form: "Have a question, concern, or feedback? We'd love to hear from you."

3. **AdminLayout.astro** — Update:
   - Add `'contact'` to the `currentPage` union type in Props interface
   - Add nav item for Contact (use mail/envelope icon) positioned after 'bugs' in the nav order
   - Add the mail SVG icon case in the nav render section

4. **contact-messages.ts** — Create GET handler at `src/pages/api/admin/contact-messages.ts`:
   - Auth check: require `context.locals.user?.isAdmin` — return 403 if not admin
   - Query: `SELECT * FROM contact_messages ORDER BY created_at DESC LIMIT 100`
   - Return JSON array of messages

5. **admin/contact.astro** — Create admin page at `src/pages/admin/contact.astro`:
   - Auth gate in Astro frontmatter (redirect non-admins to /)
   - Use AdminLayout with `currentPage="contact"` and `title="Contact Messages"`
   - Fetch contact messages from D1 directly in frontmatter (SSR, no client fetch needed — same pattern as bug-reports.astro)
   - Render a table with columns: Date, Name, Email, Category, Message (truncated), Status
   - Sort by created_at DESC (newest first)
   - Format timestamps using the project's existing date formatting pattern
   - Category displayed as colored badge (same pill style as review status badges)
   - Status column: 'new' in blue, 'resolved' in green
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>Contact page shows a working form with all fields and Turnstile widget. Admin panel has Contact tab with messages table. AdminLayout includes 'contact' in nav. Build and all tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm test -- --run` — all existing tests pass (171+)
3. Migrations 0020-0022 applied locally without errors
4. Manual: Visit /contact — form renders with name, email, category dropdown, message textarea, and Turnstile widget
5. Manual: Submit contact form — success message shown, record appears in D1 contact_messages table
6. Manual: Check email — confirmation email received by submitter
7. Manual: Visit /admin/contact as admin — table shows the submitted message
8. Manual: Visit /admin/contact as non-admin — redirected to /
9. Manual: Submit 4 times quickly — 4th submission blocked with rate limit error
</verification>

<success_criteria>
- Contact form at /contact accepts submissions from anyone (no login required)
- Submissions stored in D1 with Turnstile verification and 3/hour rate limit
- Submitter receives confirmation email from noreply@ratemyplace.org
- Admin notification email sent to contact@ratemyplace.org
- Admin panel has Contact tab showing all submissions
- Migrations 0021-0022 reserved as placeholders
- Zero regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-schema-survey-fields-and-contact-form/11-02-SUMMARY.md`
</output>
