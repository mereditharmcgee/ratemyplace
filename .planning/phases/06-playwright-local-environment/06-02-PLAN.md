---
phase: 06-playwright-local-environment
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - e2e/navigation.spec.ts
  - e2e/pages.spec.ts
autonomous: true
requirements:
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "e2e/navigation.spec.ts imports test and expect from './fixtures' instead of '@playwright/test'"
    - "e2e/pages.spec.ts imports test and expect from './fixtures' instead of '@playwright/test'"
    - "No spec file in e2e/ imports directly from '@playwright/test' (except global.setup.ts which must use @playwright/test)"
    - "All tests in navigation.spec.ts pass against local dev server (http://localhost:8788)"
    - "All tests in pages.spec.ts pass against local dev server (http://localhost:8788)"
    - "global.setup.ts creates playwright/.auth/user.json during the setup project"
    - "global.setup.ts creates playwright/.auth/admin.json during the setup project"
    - "npm run e2e completes the full pipeline: db:setup -> build -> playwright test with exit code 0"
    - "No requests to ratemyplace.org or the Cloudflare preview URL occur during the test run"
    - "workers: 1 is enforced — tests run sequentially"
  artifacts:
    - path: "e2e/navigation.spec.ts"
      provides: "Navigation E2E specs using shared fixtures import"
      contains: "from './fixtures'"
    - path: "e2e/pages.spec.ts"
      provides: "Page rendering E2E specs using shared fixtures import"
      contains: "from './fixtures'"
  key_links:
    - from: "e2e/navigation.spec.ts"
      to: "e2e/fixtures.ts"
      via: "import { test, expect }"
      pattern: "Unified test import"
    - from: "e2e/pages.spec.ts"
      to: "e2e/fixtures.ts"
      via: "import { test, expect }"
      pattern: "Unified test import"
---

<objective>
Update existing E2E spec files to use the shared fixtures import, then run the full `npm run e2e` pipeline to verify the complete Playwright local environment works end-to-end.

Purpose: Confirm that INFRA-04 (Playwright runs against local dev server) and INFRA-05 (auth fixtures create reusable sessions) are fully satisfied. Existing navigation and page tests must pass unchanged against local.

Output: 2 spec files updated with new import. Full pipeline verification via npm run e2e.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-playwright-local-environment/06-RESEARCH.md
@.planning/phases/06-playwright-local-environment/06-CONTEXT.md
@e2e/fixtures.ts (created by Plan 01)
@playwright.config.ts (updated by Plan 01)
@e2e/global.setup.ts (created by Plan 01)

<interfaces>
<!-- Current navigation.spec.ts import (line 1) -->
```typescript
import { test, expect } from '@playwright/test';
```

<!-- Current pages.spec.ts import (line 1) -->
```typescript
import { test, expect } from '@playwright/test';
```

<!-- Target import (from fixtures.ts) -->
```typescript
import { test, expect } from './fixtures';
```

<!-- fixtures.ts exports (from Plan 01) -->
```typescript
export const test = base.extend<CustomFixtures>({...});
export { expect };
```

The extended `test` preserves all default fixtures including `page`. Existing tests that destructure `{ page }` continue to work with zero changes to test bodies.

<!-- Full npm run e2e pipeline -->
npm run e2e
  -> npm run db:setup
    -> npm run db:fresh  (reset + migrate)
    -> npm run db:seed   (8 users, 10 landlords, 30 buildings, 128 reviews, 10 disputes)
  -> npm run build       (Astro build -> ./dist/)
  -> npx playwright test
    -> webServer starts: npx wrangler pages dev ./dist --port 8788
    -> setup project: global.setup.ts (user + admin sign-in -> session files)
    -> chromium project: navigation.spec.ts + pages.spec.ts

<!-- Expected test counts -->
navigation.spec.ts: ~15 tests (header nav, footer nav, cross-page links, protected redirects)
pages.spec.ts: ~12 tests (homepage, search, about, methodology, static pages, auth pages)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing spec file imports to use shared fixtures</name>
  <files>e2e/navigation.spec.ts, e2e/pages.spec.ts</files>
  <action>
Read both spec files and update the import statement on line 1 of each file.

**e2e/navigation.spec.ts:**

Change line 1 from:
```typescript
import { test, expect } from '@playwright/test';
```
To:
```typescript
import { test, expect } from './fixtures';
```

Do NOT change anything else in the file. All test bodies, describe blocks, and assertions remain exactly as they are.

**e2e/pages.spec.ts:**

Change line 1 from:
```typescript
import { test, expect } from '@playwright/test';
```
To:
```typescript
import { test, expect } from './fixtures';
```

Do NOT change anything else in the file. All test bodies, describe blocks, and assertions remain exactly as they are.

This is a mechanical two-line change across two files. The `test` export from `./fixtures` is built with `base.extend()` which preserves the default `page` fixture, so existing tests that destructure `{ page }` continue to work with zero changes.

After the edits, verify that no spec files in e2e/ (other than global.setup.ts) still import from '@playwright/test'. The convention going forward is that all specs import from './fixtures' for consistency.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const nav = fs.readFileSync('e2e/navigation.spec.ts', 'utf8'); const pages = fs.readFileSync('e2e/pages.spec.ts', 'utf8'); if (nav.includes(\"from '@playwright/test'\")) { console.error('FAIL: navigation.spec.ts still imports from @playwright/test'); process.exit(1); } if (pages.includes(\"from '@playwright/test'\")) { console.error('FAIL: pages.spec.ts still imports from @playwright/test'); process.exit(1); } if (!nav.includes(\"from './fixtures'\")) { console.error('FAIL: navigation.spec.ts not using ./fixtures'); process.exit(1); } if (!pages.includes(\"from './fixtures'\")) { console.error('FAIL: pages.spec.ts not using ./fixtures'); process.exit(1); } console.log('OK: both spec files use ./fixtures import');"</automated>
  </verify>
  <done>navigation.spec.ts and pages.spec.ts updated to import from ./fixtures instead of @playwright/test</done>
</task>

<task type="auto">
  <name>Task 2: Run full npm run e2e pipeline and verify all tests pass</name>
  <files></files>
  <action>
Run the complete E2E pipeline to verify everything works end-to-end:

```bash
npm run e2e
```

This will execute the full chain:
1. `npm run db:setup` — Reset and seed the local D1 database
2. `npm run build` — Build Astro to ./dist/
3. `npx playwright test` — Start wrangler pages dev, run setup project (global.setup.ts), run all specs

**Expected outcomes:**
- db:setup completes without errors (database reset, migrated, and seeded)
- Build produces ./dist/ directory
- wrangler pages dev starts on http://localhost:8788
- global.setup.ts setup project:
  - "create auth directory" step creates playwright/.auth/
  - "sign in as regular user" step creates playwright/.auth/user.json
  - "sign in as admin" step creates playwright/.auth/admin.json
- navigation.spec.ts: all tests pass (~15 tests)
- pages.spec.ts: all tests pass (~12 tests)
- Overall: 0 failures, exit code 0

**If tests fail, debug by checking:**
1. Did global.setup.ts fail? Check if sign-in form selectors are correct (input[name="email"], input[name="password"], button[type="submit"])
2. Did wrangler pages dev start? Check if http://localhost:8788 is responding
3. Did the build succeed? Check if ./dist/ exists and has content
4. Did db:setup succeed? Check if seed data is present
5. Are there timeout issues? The webServer has 120-second timeout; if build + wrangler startup exceeds this, increase it

**Verify auth session files were created:**
After the test run completes, check that:
- `playwright/.auth/user.json` exists and contains a non-empty JSON object
- `playwright/.auth/admin.json` exists and contains a non-empty JSON object

**Verify no production URLs were contacted:**
The test output should show all page navigations going to http://localhost:8788. No requests should go to ratemyplace.org or the Cloudflare preview URL.

IMPORTANT: This task is the integration verification for the entire phase. If it fails, debug and fix. Do not mark the plan as complete until npm run e2e exits with code 0 and all tests pass.

NOTE: If the pipeline takes more than 5 minutes on the first run, that is expected. The build step (10-20s) + wrangler startup (5-15s) + db:setup (30-60s) + test execution can add up. Subsequent runs with reuseExistingServer: true will be faster.
  </action>
  <verify>
    <automated>npm run e2e</automated>
  </verify>
  <done>npm run e2e completes the full pipeline with exit code 0. All navigation and page tests pass against local dev server. Auth session files user.json and admin.json are created by global.setup.ts. No production URLs are contacted.</done>
</task>

</tasks>

<verification>
- [ ] e2e/navigation.spec.ts imports from './fixtures'
- [ ] e2e/pages.spec.ts imports from './fixtures'
- [ ] No spec files import directly from '@playwright/test' (except global.setup.ts)
- [ ] npm run e2e executes the full pipeline without errors
- [ ] db:setup step completes (database reset, migrated, seeded)
- [ ] npm run build step produces ./dist/ directory
- [ ] wrangler pages dev starts and serves http://localhost:8788
- [ ] global.setup.ts creates playwright/.auth/user.json
- [ ] global.setup.ts creates playwright/.auth/admin.json
- [ ] All navigation.spec.ts tests pass
- [ ] All pages.spec.ts tests pass
- [ ] Overall test run: 0 failures
- [ ] No requests to production URLs during test run
</verification>

<success_criteria>
- `npm run e2e` exits with code 0
- All existing navigation and page specs pass against local dev server
- Auth session files are created by global.setup.ts
- The complete pipeline (db:setup -> build -> playwright test) works as a single command
- Existing tests required zero logic changes (only import path changed)
- INFRA-04 satisfied: Playwright runs against local dev server (http://localhost:8788)
- INFRA-05 satisfied: Auth fixtures create reusable sessions (user.json and admin.json)
</success_criteria>

<output>
After completion, create `.planning/phases/06-playwright-local-environment/06-02-SUMMARY.md`
</output>
