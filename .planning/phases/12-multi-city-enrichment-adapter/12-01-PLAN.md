---
phase: 12-multi-city-enrichment-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/enrichment/types.ts
  - src/lib/enrichment/helpers.ts
  - src/lib/enrichment/dispatcher.ts
  - src/lib/enrichment/adapters/boston.ts
  - src/lib/enrichment/adapters/null.ts
  - src/pages/api/admin/buildings/[id]/enrich.ts
  - src/lib/__tests__/enrichment.test.ts
autonomous: true
requirements: [FIX-02, ENRICH-01, ENRICH-02, ENRICH-04]

must_haves:
  truths:
    - "selectAdapter('boston') returns a BostonAdapter instance"
    - "selectAdapter(null) returns a NullAdapter, not BostonAdapter"
    - "selectAdapter('unknown city') returns a NullAdapter"
    - "BostonAdapter.enrich() returns the same response shape as the current monolithic enrich.ts"
    - "NullAdapter.enrich() returns unsupported: true with empty results array"
    - "enrich.ts endpoint is a thin dispatcher (~30 lines) that delegates to adapters"
  artifacts:
    - path: "src/lib/enrichment/types.ts"
      provides: "CityAdapter interface, EnrichResult, EnrichResponse, BuildingRecord types"
      exports: ["CityAdapter", "EnrichResult", "EnrichResponse", "BuildingRecord"]
    - path: "src/lib/enrichment/helpers.ts"
      provides: "Shared helpers extracted from enrich.ts"
      exports: ["inferOwnerEntity", "parseStreetAddress", "normalizeStreetName"]
    - path: "src/lib/enrichment/dispatcher.ts"
      provides: "City-to-adapter routing"
      exports: ["selectAdapter"]
    - path: "src/lib/enrichment/adapters/boston.ts"
      provides: "Boston Assessing API adapter"
      exports: ["BostonAdapter"]
    - path: "src/lib/enrichment/adapters/null.ts"
      provides: "Unsupported city fallback"
      exports: ["NullAdapter"]
    - path: "src/lib/__tests__/enrichment.test.ts"
      provides: "Unit tests for adapter selection, Boston adapter, and null adapter"
  key_links:
    - from: "src/pages/api/admin/buildings/[id]/enrich.ts"
      to: "src/lib/enrichment/dispatcher.ts"
      via: "selectAdapter(building.city)"
      pattern: "selectAdapter\\("
    - from: "src/lib/enrichment/dispatcher.ts"
      to: "src/lib/enrichment/adapters/boston.ts"
      via: "CITY_ADAPTERS map lookup"
      pattern: "BostonAdapter"
    - from: "src/lib/enrichment/adapters/boston.ts"
      to: "src/lib/enrichment/helpers.ts"
      via: "import inferOwnerEntity, parseStreetAddress, normalizeStreetName"
      pattern: "from.*helpers"
---

<objective>
Extract the CityAdapter interface, refactor Boston enrichment into an adapter, create the NullAdapter for unsupported cities, build the dispatcher, and replace the monolithic enrich.ts with a thin dispatcher endpoint.

Purpose: Establishes the adapter architecture so Plan 02 can add New Haven without touching existing code. Also fixes FIX-02 (city-based routing instead of always-Boston).
Output: Working adapter system with Boston behavior unchanged, unsupported cities handled gracefully, and unit tests covering adapter selection and NullAdapter.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-city-enrichment-adapter/12-RESEARCH.md

@src/pages/api/admin/buildings/[id]/enrich.ts
@src/components/admin/BuildingsTable.tsx
</context>

<interfaces>
<!-- These are the contracts this plan creates. Plan 02 will implement NewHavenAdapter against them. -->

From src/lib/enrichment/types.ts (TO BE CREATED):
```typescript
export interface EnrichResult {
  address: string;
  city: string;
  zipCode?: string;
  owner?: string;
  ownerEntityInferred?: string | null;
  yearBuilt?: number | null;
  yearRemodeled?: number | null;
  unitCount?: number | null;
  residentialUnits?: number | null;
  commercialUnits?: number | null;
  propertyType?: string;
  buildingType?: string;
  rawBuildingType?: string;
  totalValue?: number | null;
  grossArea?: number | null;
  livingArea?: number | null;
  structureClass?: string;
  overallCondition?: string;
}

export interface EnrichResponse {
  address: string;
  searchedFor?: { number: string; street: string };
  results: EnrichResult[];
  fuzzyMatch?: boolean;
  unsupported?: boolean;
  message?: string;
  source?: string;
}

export interface BuildingRecord {
  id: string;
  address: string;
  city: string | null;
  state: string | null;
  zip_code: string | null;
}

export interface CityAdapter {
  enrich(building: BuildingRecord): Promise<EnrichResponse>;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create enrichment types, helpers, dispatcher, and test scaffold</name>
  <files>
    src/lib/enrichment/types.ts,
    src/lib/enrichment/helpers.ts,
    src/lib/enrichment/dispatcher.ts,
    src/lib/enrichment/adapters/null.ts,
    src/lib/__tests__/enrichment.test.ts
  </files>
  <behavior>
    - selectAdapter('boston') returns instance of BostonAdapter
    - selectAdapter('Boston') returns instance of BostonAdapter (case insensitive)
    - selectAdapter('new haven') returns instance of NewHavenAdapter (will fail until Plan 02, but dispatcher map should include it)
    - selectAdapter(null) returns instance of NullAdapter
    - selectAdapter('springfield') returns instance of NullAdapter
    - selectAdapter('Boston, MA') returns BostonAdapter (strips trailing state abbreviation)
    - NullAdapter.enrich() returns { address: building.address, results: [], unsupported: true, message: 'No auto-research data available for this city.' }
    - inferOwnerEntity('SMITH LLC') returns 'llc'
    - inferOwnerEntity('John Smith') returns 'individual'
    - parseStreetAddress('123 Main St') returns { number: '123', street: 'Main St' }
    - parseStreetAddress('invalid') returns null
    - normalizeStreetName('Main St, Boston, MA') returns 'MAIN ST'
  </behavior>
  <action>
    1. Create `src/lib/enrichment/types.ts` with the CityAdapter interface, EnrichResult, EnrichResponse, and BuildingRecord types exactly as shown in the interfaces block above. Include a `source?: string` field on EnrichResponse for UI labeling.

    2. Create `src/lib/enrichment/helpers.ts` — extract these three functions VERBATIM from the current `src/pages/api/admin/buildings/[id]/enrich.ts`:
       - `inferOwnerEntity(ownerName: string): string | null`
       - `parseStreetAddress(address: string): { number: string; street: string } | null`
       - `normalizeStreetName(street: string): string`
       Export all three. Do NOT modify their logic.

    3. Create `src/lib/enrichment/adapters/null.ts` — NullAdapter class implementing CityAdapter. Returns `{ address: building.address, results: [], unsupported: true, message: 'No auto-research data available for this city.' }`.

    4. Create `src/lib/enrichment/dispatcher.ts` with `selectAdapter(city: string | null): CityAdapter`. City normalization: `city.replace(/,\s*[A-Z]{2}$/, '').trim().toLowerCase()`. Map keys: `'boston'` -> BostonAdapter, `'new haven'` -> NewHavenAdapter. Null/unknown -> NullAdapter. Note: NewHavenAdapter import will be added in Plan 02; for now import it but the file won't exist yet — the test for 'new haven' selection will be added in Plan 02.

    CORRECTION: Since Plan 02 creates NewHavenAdapter, the dispatcher should NOT import it yet. Instead, register only 'boston' in the map for now. Plan 02 will add the 'new haven' entry. This avoids a broken import.

    5. Create `src/lib/__tests__/enrichment.test.ts` with tests for:
       - Dispatcher: selectAdapter('boston'), selectAdapter('Boston'), selectAdapter(null), selectAdapter('springfield'), selectAdapter('Boston, MA')
       - NullAdapter: returns correct shape with unsupported: true
       - Helpers: inferOwnerEntity (LLC, trust, individual), parseStreetAddress (valid, invalid), normalizeStreetName (strips comma suffix, uppercases)

    Use Vitest. Mock fetch globally for any adapter tests that need it.
  </action>
  <verify>
    <automated>npm test -- enrichment</automated>
  </verify>
  <done>
    - types.ts exports CityAdapter, EnrichResult, EnrichResponse, BuildingRecord
    - helpers.ts exports inferOwnerEntity, parseStreetAddress, normalizeStreetName (identical logic to current enrich.ts)
    - dispatcher.ts selectAdapter routes 'boston' to BostonAdapter, null/unknown to NullAdapter
    - NullAdapter returns well-formed unsupported response
    - All enrichment tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract BostonAdapter and replace enrich.ts with thin dispatcher</name>
  <files>
    src/lib/enrichment/adapters/boston.ts,
    src/pages/api/admin/buildings/[id]/enrich.ts
  </files>
  <action>
    1. Create `src/lib/enrichment/adapters/boston.ts` — BostonAdapter class implementing CityAdapter:
       - Move `BOSTON_ASSESSING_RESOURCE_ID`, `BOSTON_ASSESSING_API`, `AssessingRecord` interface, `mapBuildingType()`, and `formatResult()` from current enrich.ts into this file.
       - Import `inferOwnerEntity`, `parseStreetAddress`, `normalizeStreetName` from `../helpers`.
       - The `enrich(building: BuildingRecord)` method contains the exact same logic as the current GET handler body (after auth/DB lookup): parse address, query Boston Assessing API with exact filters, fallback to broad search, format results. Return `EnrichResponse` with `source: 'Boston Assessing'`.
       - Preserve the exact same search logic: exact match first (filters: ST_NUM + ST_NAME), then broad fallback (q parameter), then empty results with message.
       - When address cannot be parsed, return `{ address: building.address, results: [], message: 'Could not parse address' }` (same behavior as current endpoint returning 200 with empty results).

    2. Replace `src/pages/api/admin/buildings/[id]/enrich.ts` with thin dispatcher (~30 lines):
       - Keep admin auth check, building ID validation, DB lookup (unchanged)
       - Add `state` to the building SELECT query: `SELECT id, address, city, state, zip_code FROM buildings WHERE id = ?`
       - Call `selectAdapter(building.city)` then `adapter.enrich(building)`
       - Return adapter response as JSON 200
       - Wrap in try/catch, return 500 on error with structured error JSON
       - Remove ALL Boston-specific code, helper functions, and the AssessingRecord interface from this file

    3. Add a BostonAdapter unit test to the existing enrichment.test.ts:
       - Mock global fetch to return a fake Boston Assessing API CKAN response
       - Verify BostonAdapter.enrich() returns EnrichResponse with correct field mapping
       - Verify fuzzy fallback is triggered when exact match returns empty records
  </action>
  <verify>
    <automated>npm test -- enrichment && npm run build</automated>
  </verify>
  <done>
    - BostonAdapter encapsulates all Boston Assessing API logic
    - enrich.ts is ~30 lines: auth + DB + selectAdapter + return
    - No Boston-specific code remains in enrich.ts
    - All enrichment tests pass
    - Build succeeds (TypeScript compiles clean)
    - Boston enrichment behavior is functionally identical to before refactor
  </done>
</task>

</tasks>

<verification>
- `npm test -- enrichment` passes all adapter selection, NullAdapter, helper, and BostonAdapter tests
- `npm run build` succeeds with no TypeScript errors
- `npm test` full suite passes (no regressions to existing 189+ tests)
</verification>

<success_criteria>
- CityAdapter interface defined and exported
- BostonAdapter extracted with identical behavior to current monolithic endpoint
- NullAdapter returns well-formed unsupported response
- Dispatcher routes by city column with case-insensitive normalization
- enrich.ts is a thin dispatcher with no city-specific logic
- Unit tests cover adapter selection, NullAdapter behavior, helper functions, and BostonAdapter field mapping
</success_criteria>

<output>
After completion, create `.planning/phases/12-multi-city-enrichment-adapter/12-01-SUMMARY.md`
</output>
