---
phase: 19-d1-index-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - migrations/0024_perf_indexes.sql
  - .planning/audits/d1-indexes-2026-04-28.md
autonomous: true
requirements:
  - PERF-05
  - PERF-06
  - PERF-07
must_haves:
  truths:
    - "The composite index `reviews(building_id, status)` exists in production D1"
    - "The migration `0024_perf_indexes.sql` is recorded as applied on production via `wrangler d1 migrations list`"
    - "Post-migration EXPLAIN QUERY PLAN against production for Queries 1-4 is captured in the audit doc"
    - "Each post-migration EXPLAIN result is interpreted: either confirms the new composite is used OR documents that the planner kept the old plan (acceptable observation per research Pitfall 3)"
    - "`npm test` and `npm run build` both pass — no regression introduced"
  artifacts:
    - path: "migrations/0024_perf_indexes.sql"
      provides: "Idempotent CREATE INDEX statements for confirmed-necessary indexes plus block-comment header listing skips"
      contains: "CREATE INDEX IF NOT EXISTS idx_reviews_building_status"
      min_lines: 10
    - path: ".planning/audits/d1-indexes-2026-04-28.md"
      provides: "Audit doc populated with after-EXPLAIN sections for each added index"
      contains: "EXPLAIN QUERY PLAN (after"
  key_links:
    - from: "migrations/0024_perf_indexes.sql"
      to: ".planning/audits/d1-indexes-2026-04-28.md"
      via: "block-comment header inline reference"
      pattern: "Audit: \\.planning/audits/d1-indexes-2026-04-28\\.md"
    - from: "migrations/0024_perf_indexes.sql"
      to: "production D1 schema"
      via: "wrangler d1 migrations apply --remote"
      pattern: "0024_perf_indexes"
---

<objective>
Write `migrations/0024_perf_indexes.sql` based on Plan 19-01's Decisions Summary, apply it locally then remotely (with `PRAGMA optimize` after each apply), and re-run EXPLAIN QUERY PLAN to populate the after-state sections of the audit doc. This plan ships PERF-06 (composite index) and finalizes PERF-05 (EXPLAIN evidence) and PERF-07 (skip indexes documented in migration header).

Purpose: PERF-06 is unconditional and PERF-07 is conditional — both must land in the same migration file with the conditional decisions visible in the block-comment header. The post-migration EXPLAIN re-run closes the audit and proves the migration achieved its goal.

Output: A new SQL migration file applied to local + remote D1, plus the audit doc updated with after-state EXPLAIN evidence per added index.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-d1-index-migration/19-CONTEXT.md
@.planning/phases/19-d1-index-migration/19-RESEARCH.md
@.planning/phases/19-d1-index-migration/19-VALIDATION.md
@.planning/phases/19-d1-index-migration/19-01-PLAN.md
@.planning/phases/19-d1-index-migration/19-01-SUMMARY.md
@.planning/audits/d1-indexes-2026-04-28.md
@CLAUDE.md
@migrations/0023_saved_buildings.sql
@migrations/0015_password_reset_tokens.sql

<interfaces>
<!-- Established migration conventions (from 19-CONTEXT.md and migrations/ inspection): -->
<!-- 1. Filename: NNNN_description.sql, next number is 0024 -->
<!-- 2. SQL keywords UPPERCASE (CREATE INDEX, IF NOT EXISTS) -->
<!-- 3. Idempotent: CREATE INDEX IF NOT EXISTS pattern (already used in 0006/0010/0011/0012/0013/0015) -->
<!-- 4. Block-comment header at top, multi-line, hyphens for list items -->
<!-- 5. Index naming: idx_<table>_<col1>_<col2>... -->

<!-- Apply commands (locked in 19-CONTEXT.md "Apply order"): -->
npx wrangler d1 migrations apply ratemyplace-db --local
npx wrangler d1 execute ratemyplace-db --local --command 'PRAGMA optimize'
npx wrangler d1 migrations apply ratemyplace-db --remote
npx wrangler d1 execute ratemyplace-db --remote --command 'PRAGMA optimize'

<!-- Heredoc command pattern for any EXPLAIN with embedded `'approved'` (from research): -->
npx wrangler d1 execute ratemyplace-db --remote --command "$(cat <<'SQL'
EXPLAIN QUERY PLAN
SELECT ... AND r.status = 'approved' ...
SQL
)"
<!-- Never use --file with --remote (wrangler bug #8020). -->

<!-- Schema introspection pattern for sqlite_master verification: -->
npx wrangler d1 execute ratemyplace-db --remote --command "SELECT name FROM sqlite_master WHERE type='index' AND name='idx_reviews_building_status'"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migrations/0024_perf_indexes.sql per the audit doc Decisions Summary</name>
  <files>migrations/0024_perf_indexes.sql</files>
  <action>
Read the Decisions Summary section of `.planning/audits/d1-indexes-2026-04-28.md` (written in Plan 19-01). Translate it directly into `migrations/0024_perf_indexes.sql`. The audit doc IS the spec for this file — do not re-derive verdicts; consume them.

**Required content structure:**

```sql
-- Phase 19: Performance indexes
-- Audit: .planning/audits/d1-indexes-2026-04-28.md
-- Added:
--   * reviews(building_id, status) — composite for search join (PERF-06, unconditional)
--   * <list any conditional indexes that were ADDED per audit Decisions Summary, e.g. rate_limits(rate_key, created_at) if Query 4 EXPLAIN warranted it>
-- Skipped (with reason):
--   * buildings(city) — no SELECT WHERE city = ? queries found in src/ (grep evidence in audit doc Query 5)
--   * buildings(building_type) — no SELECT WHERE building_type = ? queries found in src/ (grep evidence in audit doc Query 5)
--   * <list any other skipped indexes with one-sentence reason, e.g. rate_limits(rate_key, created_at) — existing idx_rate_limits_key already covers the equality predicate per Query 4 EXPLAIN>

CREATE INDEX IF NOT EXISTS idx_reviews_building_status ON reviews(building_id, status);

-- Conditional CREATE INDEX statements appear here ONLY if the audit Decisions Summary
-- marked them as ADDED. If the audit marked them SKIPPED, they appear ONLY in the
-- block-comment "Skipped" list above, NOT as SQL statements.
```

**Naming (locked per 19-CONTEXT.md):**
- Composite: `idx_reviews_building_status` ON `reviews(building_id, status)` — column order matters; building_id first because it is the join column with higher selectivity, status second to act as the filter predicate per leftmost-prefix rule (cite SQLite query planner behavior).
- Conditional bonus (only if audit said add): `idx_rate_limits_key_created` ON `rate_limits(rate_key, created_at)`.

**Hard rules:**
- Use `CREATE INDEX IF NOT EXISTS` for every index — matches existing migration convention (verified across 0006/0010/0011/0012/0013/0015).
- UPPERCASE SQL keywords. Existing migrations 0017-0023 follow this; do not lowercase.
- Block-comment header MUST list every conditional index considered, even ones skipped. Future engineers reading this migration must learn from the comment alone what was considered and why each verdict was reached. Two-place pattern (audit doc + migration header) is from CONTEXT.md.
- The migration MUST contain at least one CREATE INDEX statement (PERF-06 is unconditional). An empty migration file is a planning bug, not a valid output.
- Do NOT include covering indexes (`reviews(building_id, status, overall_score)`) — explicitly deferred to v1.6.0 per CONTEXT.md "Out of scope" and Specifics.
- Do NOT add `idx_buildings_city` or `idx_buildings_building_type` as CREATE INDEX statements — research-confirmed grep shows zero filter queries; they appear in the "Skipped" comment list only.
- Do NOT modify any other migration file. 0024 is a new file at the next sequential number.
- Do NOT touch app code. This phase is schema + docs.

**Reference existing migration for style:** Open `migrations/0023_saved_buildings.sql` for an example of the established header-comment + UPPERCASE SQL + IF NOT EXISTS pattern.
  </action>
  <verify>
    <automated>test -f migrations/0024_perf_indexes.sql && grep -q "CREATE INDEX IF NOT EXISTS idx_reviews_building_status ON reviews(building_id, status)" migrations/0024_perf_indexes.sql && grep -q "Audit: .planning/audits/d1-indexes-2026-04-28.md" migrations/0024_perf_indexes.sql && grep -q "Skipped" migrations/0024_perf_indexes.sql</automated>
  </verify>
  <done>
File `migrations/0024_perf_indexes.sql` exists with: (a) block-comment header citing the audit doc path, (b) "Added" list naming each CREATE INDEX statement included, (c) "Skipped" list naming each index considered-but-skipped with one-sentence reason, (d) at minimum the unconditional `CREATE INDEX IF NOT EXISTS idx_reviews_building_status ON reviews(building_id, status);` statement, (e) any conditional indexes whose audit verdict was ADD (and only those). UPPERCASE SQL keywords throughout. No covering indexes. No app code touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply migration local-then-remote with PRAGMA optimize and EXPLAIN re-run at each stage</name>
  <files>(no files modified — applies migration to D1 and captures EXPLAIN output for use in Task 3)</files>
  <action>
Apply the migration to local D1 first as a sanity check, then to production. Run `PRAGMA optimize` after each apply per research Pitfall 3 (without it, SQLite planner may not pick up the new composite via ANALYZE statistics). Capture before/after EXPLAIN output for Task 3 to embed in the audit doc.

**Stage A — LOCAL** (always do this first; if any step fails, fix and retry — do NOT proceed to Stage B until local is clean):

A1. Apply migration locally:
```bash
npx wrangler d1 migrations apply ratemyplace-db --local
```
Expected: wrangler reports `0024_perf_indexes` applied.

A2. Run PRAGMA optimize:
```bash
npx wrangler d1 execute ratemyplace-db --local --command 'PRAGMA optimize'
```

A3. Verify index exists in local schema:
```bash
npx wrangler d1 execute ratemyplace-db --local --command "SELECT name FROM sqlite_master WHERE type='index' AND name='idx_reviews_building_status'"
```
Expected: 1 row. Repeat for each conditional index that the migration added (e.g., `idx_rate_limits_key_created` if applicable).

A4. Re-run EXPLAIN QUERY PLAN locally for each "Add" query in the audit doc (Queries 1-4, only those with an Add verdict — heredoc pattern, `--local`). Capture verbatim output for Task 3.

Local data may be sparse — planner may or may not pick the new composite (research Open Question 2). Both outcomes are acceptable observations.

**Stage B — REMOTE / PRODUCTION** (only after Stage A completes cleanly):

B1. Apply migration remotely:
```bash
npx wrangler d1 migrations apply ratemyplace-db --remote
```
Expected: wrangler reports `0024_perf_indexes` applied to production. If wrangler resolves the wrong database id (research Open Question 3, GitHub issue #9099), retry with `--database-id <uuid>` (UUID is in `wrangler.toml`).

B2. Run PRAGMA optimize on production:
```bash
npx wrangler d1 execute ratemyplace-db --remote --command 'PRAGMA optimize'
```

B3. Confirm migration is in the applied list:
```bash
npx wrangler d1 migrations list ratemyplace-db --remote
```
Expected: `0024_perf_indexes` appears with a recent timestamp.

B4. Verify production index exists:
```bash
npx wrangler d1 execute ratemyplace-db --remote --command "SELECT name FROM sqlite_master WHERE type='index' AND name='idx_reviews_building_status'"
```
Expected: 1 row. Repeat for each conditional index added.

B5. Run `PRAGMA index_list('reviews')` to confirm the composite shows in production (mirrors ROADMAP.md success criterion #2):
```bash
npx wrangler d1 execute ratemyplace-db --remote --command "PRAGMA index_list('reviews')"
```
Expected output includes `idx_reviews_building_status`.

B6. Re-run EXPLAIN QUERY PLAN on production for each "Add" query (heredoc pattern, `--remote`). Capture verbatim output for Task 3 — this is the authoritative after-state evidence.

**Stage C — Regression gate** (run after Stage B succeeds):

C1. `npm test` — schema-only changes should not affect tests, but the regression gate must stay GREEN.

C2. `npm run build` — schema changes shouldn't ripple, but a stale type cache or env binding could surface here.

**Hard rules:**
- Stage B must NOT start until Stage A's verification (A1-A4) passes.
- PRAGMA optimize MUST run after every apply — research Pitfall 3 explicitly warns about ignored composites without it.
- All wrangler EXPLAIN commands use `--command "$(cat <<'SQL' ... SQL)"` heredoc — never `--file --remote` (bug #8020).
- Do NOT modify any data on production beyond the migration's CREATE INDEX statements. No UPDATE/DELETE/INSERT.
- Do NOT modify any other migration file. Only 0024 is in scope.
- If production EXPLAIN still shows the planner picking the old single-col `idx_reviews_building` instead of the new composite (per research Pitfall 3): that is an acceptable observation. The composite is still present and will be used as data grows. Document in Task 3, do not treat as failure.
  </action>
  <verify>
    <automated>npx wrangler d1 execute ratemyplace-db --remote --command "SELECT name FROM sqlite_master WHERE type='index' AND name='idx_reviews_building_status'" 2>&1 | grep -q "idx_reviews_building_status"</automated>
  </verify>
  <done>
Migration `0024_perf_indexes` applied to BOTH local and production D1 (confirmed via `wrangler d1 migrations list` for each). `sqlite_master` SELECT confirms `idx_reviews_building_status` exists in both local and production schemas (and any conditional indexes added). `PRAGMA index_list('reviews')` on production includes the new composite. `PRAGMA optimize` ran without error on both. Production EXPLAIN QUERY PLAN output for Queries 1-4 captured verbatim for Task 3. `npm test` and `npm run build` both pass — no regression introduced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update audit doc with after-state EXPLAIN evidence and migration application log</name>
  <files>.planning/audits/d1-indexes-2026-04-28.md</files>
  <action>
Open `.planning/audits/d1-indexes-2026-04-28.md` (created in Plan 19-01) and populate the "EXPLAIN QUERY PLAN (after)" placeholders for every query that had an index added in Task 2.

**For each of Queries 1-4 in the audit doc:**

1. If the query's verdict was "Add" — replace the `*Captured in Plan 19-02 after migration applied.*` placeholder with two code-fenced blocks plus an observation:

```markdown
**EXPLAIN QUERY PLAN (after, local):**
\`\`\`
[verbatim wrangler output captured in Task 2 Stage A4]
\`\`\`

**EXPLAIN QUERY PLAN (after, remote):**
\`\`\`
[verbatim wrangler output captured in Task 2 Stage B6]
\`\`\`

**Observation:** [One short paragraph stating whether the planner picked up the new composite (e.g., `SEARCH reviews USING INDEX idx_reviews_building_status`) or kept the old plan (e.g., `SEARCH reviews USING INDEX idx_reviews_building`). Per research Pitfall 3, both outcomes are acceptable on small-data datasets — note which was observed. If planner kept old index despite PRAGMA optimize, document that the composite is still in place and will be used as data grows.]
```

2. If the query's verdict was "Skip" — leave the after section as-is (no after-EXPLAIN needed; the existing index already covered it).

**Add a "Migration Application Log" section near the end of the audit doc:**

```markdown
## Migration Application Log

- **Migration file:** `migrations/0024_perf_indexes.sql`
- **Local apply:** [date/time, command output summary — e.g., "0024_perf_indexes applied at 2026-04-28 16:55 UTC, 1 statement executed"]
- **Local PRAGMA optimize:** ran without error
- **Local sqlite_master verification:** idx_reviews_building_status present (and list any other added indexes)
- **Remote apply:** [date/time, command output summary]
- **Remote PRAGMA optimize:** ran without error
- **Remote sqlite_master verification:** idx_reviews_building_status present in production
- **Remote `PRAGMA index_list('reviews')`:** includes idx_reviews_building_status
- **Remote `wrangler d1 migrations list`:** 0024_perf_indexes shown as applied
- **`npm run build`:** GREEN, no regression
- **`npm test`:** GREEN, no regression
```

**Update the Summary Table at the top of the audit doc** if any verdict changed during execution (e.g., a borderline rate_limits decision flipped after seeing actual remote EXPLAIN output). The summary table must reflect what actually happened, not what was predicted in Plan 19-01.

**Hard rules:**
- Do NOT delete the before-EXPLAIN sections or modify the original Decisions Summary written in Plan 19-01 — append after-state, do not rewrite history.
- Do NOT modify the migration file in this task — Task 1 owns it. If Task 2 revealed a verdict change (e.g., the rate_limits composite turned out unnecessary), the migration file should already reflect that from Task 1; if a discrepancy exists between audit doc and migration file, the audit doc is the source of truth and the migration file is wrong — return to Task 1.
- The audit doc is a snapshot once finalized in this task. After this, the doc is frozen; future re-audits create new files (e.g., `d1-indexes-2026-10-15.md`).
- Per CLAUDE.md: only commit when explicitly asked. The orchestrator handles git per the GSD workflow — do not autonomously commit unless instructed.
  </action>
  <verify>
    <automated>grep -c "EXPLAIN QUERY PLAN (after" .planning/audits/d1-indexes-2026-04-28.md | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -q "Migration Application Log" .planning/audits/d1-indexes-2026-04-28.md && npm test 2>&1 | tail -10 | grep -qE "passed|PASS|Tests "</automated>
  </verify>
  <done>
Audit doc `.planning/audits/d1-indexes-2026-04-28.md` is complete: every "Add" query has both local and remote after-EXPLAIN sections populated with verbatim wrangler output and a one-paragraph observation; "Migration Application Log" section captures the apply timeline + verification steps including `PRAGMA index_list('reviews')` evidence; Summary Table reflects actual verdicts. `npm test` passes (regression gate). Production has the composite index, the audit doc proves it, and PERF-05/PERF-06/PERF-07 are all satisfied with documented evidence.
  </done>
</task>

</tasks>

<verification>
- File exists: `migrations/0024_perf_indexes.sql` with block-comment header, `CREATE INDEX IF NOT EXISTS idx_reviews_building_status`, and "Skipped" list with reasons.
- `wrangler d1 migrations list ratemyplace-db --remote` shows `0024_perf_indexes` as applied.
- `wrangler d1 execute ratemyplace-db --remote --command "SELECT name FROM sqlite_master WHERE type='index' AND name='idx_reviews_building_status'"` returns 1 row.
- `wrangler d1 execute ratemyplace-db --remote --command "PRAGMA index_list('reviews')"` includes `idx_reviews_building_status` (mirrors ROADMAP.md success criterion #2).
- Audit doc has at least one "EXPLAIN QUERY PLAN (after, remote)" section with verbatim wrangler output, plus the "Migration Application Log" section.
- `npm test` passes (regression gate — no app code touched, all unit tests should still be GREEN).
- `npm run build` passes (schema-only change should not break build).
</verification>

<success_criteria>
1. PERF-05 fully satisfied: EXPLAIN QUERY PLAN evidence captured BEFORE (Plan 19-01) AND AFTER (this plan) the migration for every query that had an index added.
2. PERF-06 fully satisfied: composite index `reviews(building_id, status)` named `idx_reviews_building_status` is present in production D1 schema, verified by both `sqlite_master` query and `PRAGMA index_list('reviews')`.
3. PERF-07 fully satisfied: `buildings(city)` and `buildings(building_type)` indexes are correctly skipped, with grep evidence quoted in the audit doc and a one-line skip reason in the migration block-comment header.
4. ROADMAP.md success criteria #1 met: EXPLAIN output captured before migration (in Plan 19-01).
5. ROADMAP.md success criteria #2 met: composite index present in production schema (verified via `PRAGMA index_list('reviews')`).
6. ROADMAP.md success criteria #3 met: city/building_type indexes either present (with EXPLAIN evidence) OR explicitly skipped (with EXPLAIN/grep evidence in migration file comment).
7. No regression: `npm test` and `npm run build` both pass post-migration.
</success_criteria>

<output>
After completion, create `.planning/phases/19-d1-index-migration/19-02-SUMMARY.md`
</output>
