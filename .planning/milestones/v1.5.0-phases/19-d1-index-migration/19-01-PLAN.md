---
phase: 19-d1-index-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audits/d1-indexes-2026-04-28.md
autonomous: true
requirements:
  - PERF-05
  - PERF-07
must_haves:
  truths:
    - "Each of the 5 hot-path queries has its EXPLAIN QUERY PLAN output captured against production D1"
    - "Each query has a written decision (add index / skip + reason) backed by SCAN-vs-SEARCH evidence"
    - "PERF-07 (city/building_type indexes) decision is grounded in grep evidence quoted directly in the audit doc"
    - "The audit doc names every conditional index considered (rate_limits composite, buildings city, buildings building_type) and records the verdict for each"
  artifacts:
    - path: ".planning/audits/d1-indexes-2026-04-28.md"
      provides: "Per-query EXPLAIN audit with decisions and grep evidence"
      contains: "EXPLAIN QUERY PLAN"
      min_lines: 100
  key_links:
    - from: ".planning/audits/d1-indexes-2026-04-28.md"
      to: "src/pages/api/search/results.ts"
      via: "file:line reference for Query 1 and Query 2 SQL"
      pattern: "src/pages/api/search/results\\.ts:"
    - from: ".planning/audits/d1-indexes-2026-04-28.md"
      to: "src/pages/api/search/autocomplete.ts"
      via: "file:line reference for Query 3 SQL"
      pattern: "src/pages/api/search/autocomplete\\.ts:"
    - from: ".planning/audits/d1-indexes-2026-04-28.md"
      to: "src/lib/rateLimit.ts"
      via: "file:line reference for Query 4 SQL"
      pattern: "src/lib/rateLimit\\.ts:"
---

<objective>
Run `EXPLAIN QUERY PLAN` against the 5 locked hot-path queries on production D1 and write the audit document at `.planning/audits/d1-indexes-2026-04-28.md` with per-query evidence and decisions. No schema changes occur in this plan — output is documentation only that drives Plan 19-02's migration content.

Purpose: PERF-05 requires EXPLAIN evidence before writing PERF-06/07 indexes. The audit doc is the canonical record of what plans were running before the migration and which indexes were judged necessary.

Output: A populated audit document containing 5 per-query sections (or 4 + a discovery section if Query 5 is grep-only) with code-fenced SCAN/SEARCH output and explicit decisions for each candidate index.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-d1-index-migration/19-CONTEXT.md
@.planning/phases/19-d1-index-migration/19-RESEARCH.md
@.planning/phases/19-d1-index-migration/19-VALIDATION.md
@CLAUDE.md
@src/pages/api/search/results.ts
@src/pages/api/search/autocomplete.ts
@src/lib/rateLimit.ts
@.planning/audits/csrf-2026-04.md

<interfaces>
<!-- Audit doc convention reference: Phase 18's csrf-2026-04.md is the prior audit-doc example. Follow its overall shape (frontmatter date, sections with evidence-quoted output) but use the per-query structure decided in 19-CONTEXT.md. -->

<!-- Existing index inventory (from 19-CONTEXT.md, code_context section) — these already exist, do NOT propose duplicates: -->
reviews:
  - idx_reviews_building (building_id)
  - idx_reviews_user (user_id)
  - idx_reviews_status (status)
  - idx_reviews_created (created_at)
buildings:
  - idx_buildings_slug
  - idx_buildings_landlord
  - idx_buildings_neighborhood
  - idx_buildings_address
rate_limits:
  - idx_rate_limits_key (rate_key)
  - idx_rate_limits_expires (expires_at)

<!-- Heredoc command pattern (from research, Pitfall 1 + Strategy B) — use this for any EXPLAIN with embedded `'approved'` literal: -->
npx wrangler d1 execute ratemyplace-db --remote --command "$(cat <<'SQL'
EXPLAIN QUERY PLAN
SELECT ... AND r.status = 'approved' ...
SQL
)"
<!-- Never use --file with --remote (wrangler bug #8020). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run EXPLAIN QUERY PLAN audit against production D1 for all 5 hot-path queries</name>
  <files>(no files modified — capture command output for use in Task 2)</files>
  <action>
Run `EXPLAIN QUERY PLAN` against production D1 for the 5 hot-path queries listed in 19-RESEARCH.md "Code Examples" section. Use the heredoc-to-`--command` pattern (Strategy B from research) for EVERY EXPLAIN — never use single-quoted shell strings with embedded `'approved'`, never use `--file --remote` (wrangler bug #8020).

For each query, capture the raw wrangler output verbatim (including the SEARCH/SCAN lines). Save into a scratch buffer for Task 2 to embed in the audit doc.

**Query 1 — Search results buildings (`src/pages/api/search/results.ts:44-54`):**
```bash
npx wrangler d1 execute ratemyplace-db --remote --command "$(cat <<'SQL'
EXPLAIN QUERY PLAN
SELECT b.*, COUNT(r.id) as review_count, ROUND(AVG(r.overall_score), 1) as avg_overall, l.name as landlord_name
FROM buildings b
LEFT JOIN reviews r ON b.id = r.building_id AND r.status = 'approved'
LEFT JOIN landlords l ON b.landlord_id = l.id
GROUP BY b.id
HAVING COUNT(r.id) > 0
ORDER BY COUNT(r.id) DESC, AVG(r.overall_score) DESC
LIMIT 10 OFFSET 0
SQL
)"
```

**Query 2 — Search results landlords (`src/pages/api/search/results.ts:78-89`):** Same heredoc pattern, query body from research "Code Examples" Query 2.

**Query 3 — Autocomplete buildings (`src/pages/api/search/autocomplete.ts:49-60`):** Same pattern, query body from research Query 3.

**Query 4 — Rate-limit COUNT (`src/lib/rateLimit.ts:39-41`):** Same pattern, query body from research Query 4. Use representative literal values: `rate_key = 'search-results:1.2.3.4'` and `created_at > 1714000000`.

**Query 5 — PERF-07 filter discovery:** Run grep instead of EXPLAIN. Capture the exact output:
```bash
grep -rn 'city =\|building_type =' src/
```
This is the definitive evidence for PERF-07. Per research, this should return only the two admin PATCH UPDATE SET lines in `src/pages/api/admin/buildings/[id].ts` — zero SELECT WHERE results. If anything else appears (a new SELECT WHERE filter has been added since research), the result changes the verdict and Task 2 must reflect it.

**Bonus — rate_limits composite consideration:** Query 4's EXPLAIN output is the deciding evidence for whether to add `idx_rate_limits_key_created`. Per CONTEXT.md skip rule: if output shows `SEARCH rate_limits USING INDEX idx_rate_limits_key (rate_key=?)`, the existing single-col index already covers the equality predicate — record skip. If output shows `SCAN TABLE rate_limits` (no USING INDEX), record add. Ambiguity tilts toward add.

**If wrangler `--remote` fails with a database-id resolution error** (open question 3 from research, GitHub issue #9099): re-run with `--database-id <uuid>` instead of the database name. UUID is in `wrangler.toml`.

Do NOT apply any migrations in this task. Read-only EXPLAIN is safe against production. The next plan (19-02) writes and applies the migration.
  </action>
  <verify>
    <automated>npx wrangler d1 execute ratemyplace-db --remote --command "SELECT 1 as ok" | grep -q "ok"</automated>
  </verify>
  <done>
All 5 EXPLAIN/grep outputs captured verbatim from production D1 (and grep against src/). For each query: SEARCH or SCAN classification clearly visible in output. Skip/add verdict determined per query against the locked rule (`SEARCH ... USING INDEX` = skip, `SCAN TABLE` = add, ambiguous = add). PERF-07 verdict determined from grep output (research-predicted: skip both with zero-filter-queries evidence). Rate-limits composite verdict determined from Query 4 output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the audit document with per-query EXPLAIN evidence and decisions</name>
  <files>.planning/audits/d1-indexes-2026-04-28.md</files>
  <action>
Create `.planning/audits/d1-indexes-2026-04-28.md` documenting every EXPLAIN result and decision from Task 1. The doc is a snapshot — never re-edited later; future re-audits create new files.

**Required structure (per 19-CONTEXT.md "Audit document" decision):**

```markdown
# D1 Index Audit — 2026-04-28

**Researcher:** Phase 19 planner (Plan 19-01)
**Source:** `EXPLAIN QUERY PLAN` against production D1 (`wrangler d1 execute ratemyplace-db --remote`)
**Companion migration:** `migrations/0024_perf_indexes.sql` (written in Plan 19-02)
**Skip threshold:** Per 19-CONTEXT.md — `SEARCH ... USING INDEX` = existing index sufficient (skip); `SCAN TABLE <name>` (no USING INDEX) = full scan (add); ambiguous = default to add. Cite SQLite EXPLAIN docs (https://www.sqlite.org/eqp.html).

## Summary Table

| # | Query | Before Plan | Verdict | Index |
|---|-------|-------------|---------|-------|
| 1 | Search results — buildings JOIN approved reviews | (paste from Task 1, e.g. SCAN reviews / SEARCH reviews USING INDEX idx_reviews_building) | Add | idx_reviews_building_status |
| 2 | Search results — landlords JOIN buildings JOIN approved reviews | ... | Add (same composite — same join) | idx_reviews_building_status (shared) |
| 3 | Autocomplete buildings | ... | Add (same composite — same join) | idx_reviews_building_status (shared) |
| 4 | Rate-limit COUNT | (paste verdict) | Add OR Skip | idx_rate_limits_key_created OR — |
| 5 | buildings(city) / buildings(building_type) filters | grep: 0 SELECT WHERE matches | Skip both | — |

## Query 1 — Search results: buildings LEFT JOIN approved reviews

**Source:** `src/pages/api/search/results.ts:44-54`

**SQL:**
\`\`\`sql
SELECT b.*, COUNT(r.id) as review_count, ROUND(AVG(r.overall_score), 1) as avg_overall, l.name as landlord_name
FROM buildings b
LEFT JOIN reviews r ON b.id = r.building_id AND r.status = 'approved'
LEFT JOIN landlords l ON b.landlord_id = l.id
GROUP BY b.id
HAVING COUNT(r.id) > 0
ORDER BY COUNT(r.id) DESC, AVG(r.overall_score) DESC
LIMIT 10 OFFSET 0
\`\`\`

**EXPLAIN QUERY PLAN (before):**
\`\`\`
[paste raw wrangler output from Task 1 verbatim]
\`\`\`

**Decision:** [Add idx_reviews_building_status | Skip — already covered by idx_reviews_building]

**Reasoning:** [Quote the specific SCAN-vs-SEARCH line that drove the verdict. If SCAN TABLE reviews appears, note it; if SEARCH reviews USING INDEX idx_reviews_building (no status), explain that the composite still satisfies both predicates with one lookup vs. row-filter-after-index. Cite SQLite one-index-per-table planner rule.]

**EXPLAIN QUERY PLAN (after):** *Captured in Plan 19-02 after migration applied.*
```

Repeat the per-query block (Source / SQL / Before / Decision / Reasoning / After-placeholder) for Queries 2, 3, and 4.

**Query 5 section is structurally different** (no EXPLAIN — grep evidence only):

```markdown
## Query 5 — PERF-07 filter query discovery (buildings.city, buildings.building_type)

**Discovery method:** `grep -rn 'city =\|building_type =' src/`

**Result (verbatim):**
\`\`\`
[paste exact grep output from Task 1]
\`\`\`

**Analysis:** [Confirm whether all matches are UPDATE SET clauses (admin PATCH at `src/pages/api/admin/buildings/[id].ts:72,92`) — research-predicted outcome. Note that UPDATE SET assignments do not benefit from indexes on those columns; only WHERE filters do. If a SELECT WHERE city = ? or WHERE building_type = ? line appears (would change verdict), document it and reverse to "add".]

**Decision:** Skip both `idx_buildings_city` and `idx_buildings_building_type` — PERF-07 not applicable. No SELECT WHERE filter queries exist on these columns in `src/`.

**Future trigger:** If a city or building_type filter feature ships in v1.6.0 or later, re-audit and add the indexes then. This is not "indexes are bad here" — it is "no query exists yet for these indexes to help."
```

**Conditional indexes summary** at the bottom of the doc:

```markdown
## Decisions Summary (for migrations/0024_perf_indexes.sql)

**Always added:**
- `idx_reviews_building_status` ON reviews(building_id, status) — PERF-06 unconditional, used by Queries 1, 2, 3.

**Conditionally added (decision per Task 1 output):**
- `idx_rate_limits_key_created` ON rate_limits(rate_key, created_at) — [ADD or SKIP] based on Query 4 EXPLAIN.

**Skipped (with reason):**
- `idx_buildings_city` — no SELECT WHERE city = ? queries in src/ (Query 5 grep evidence above).
- `idx_buildings_building_type` — no SELECT WHERE building_type = ? queries in src/ (Query 5 grep evidence above).
- [If rate_limits composite is skipped:] `idx_rate_limits_key_created` — existing `idx_rate_limits_key` already covers the equality predicate; Query 4 EXPLAIN shows `SEARCH ... USING INDEX idx_rate_limits_key`, no plan change needed.

## After-state placeholder

The "EXPLAIN QUERY PLAN (after)" sections are populated by Plan 19-02 once the migration is applied locally and remotely. PRAGMA optimize must be run before the after-EXPLAIN per 19-RESEARCH.md Pitfall 3.
```

**Optional sections (Claude's Discretion per CONTEXT.md):**
- A separate audit of the COUNT subquery in `/api/search/results` if useful — research notes it shares the join with Query 1 but has different ORDER BY.
- `EXPLAIN` (without QUERY PLAN) for any query where bonus depth is informative.

**Do NOT:**
- Use any libraries other than wrangler. Do not add Zod or any validation lib (out of scope per REQUIREMENTS.md).
- Edit existing audit docs (`.planning/audits/csrf-2026-04.md` is a separate snapshot — leave it alone).
- Modify the migration file `migrations/0024_perf_indexes.sql` — that is Plan 19-02's exclusive territory.
- Run `wrangler d1 migrations apply` — only Plan 19-02 applies migrations.
- Use `wrangler d1 execute --file --remote` (bug #8020). Always use `--command "$(cat <<'SQL' ... SQL)"` heredoc.
  </action>
  <verify>
    <automated>test -f .planning/audits/d1-indexes-2026-04-28.md && grep -q "EXPLAIN QUERY PLAN" .planning/audits/d1-indexes-2026-04-28.md && grep -q "idx_reviews_building_status" .planning/audits/d1-indexes-2026-04-28.md && grep -qE "city =|building_type =" .planning/audits/d1-indexes-2026-04-28.md</automated>
  </verify>
  <done>
Audit doc exists at `.planning/audits/d1-indexes-2026-04-28.md` with: (a) per-query section for each of Queries 1-4 containing source file:line reference, code-fenced SQL, code-fenced EXPLAIN output verbatim, decision, and reasoning quoting the SCAN/SEARCH evidence; (b) Query 5 section with raw grep output and PERF-07 skip verdict; (c) Decisions Summary listing the always-added composite, the conditional rate_limits verdict, and the skipped indexes with reasons; (d) After-state placeholder section noting Plan 19-02 will populate it. Doc is a complete snapshot ready for Plan 19-02 to consume as the migration spec.
  </done>
</task>

</tasks>

<verification>
- File exists: `.planning/audits/d1-indexes-2026-04-28.md`
- All 5 audited items have a section in the doc (Queries 1-4 with EXPLAIN, Query 5 with grep)
- Decisions Summary section enumerates: always-added composite, conditional indexes (verdict each), and skipped indexes (with reason each)
- No migration file written, no migration applied — schema unchanged on local and remote
- `npm test` still GREEN (regression gate — no app code touched)
</verification>

<success_criteria>
1. PERF-05 partially satisfied: EXPLAIN QUERY PLAN evidence captured for all 5 hot-path queries against production D1.
2. PERF-07 verdict locked with documented evidence: skip both `buildings(city)` and `buildings(building_type)` indexes, citing the grep output that proves zero SELECT WHERE filter queries exist on these columns in `src/`.
3. Rate-limits composite verdict locked with EXPLAIN evidence (add or skip with reason).
4. Plan 19-02 has a complete spec to consume — it knows exactly which `CREATE INDEX` statements to write and which skips to list in the migration block-comment header.
</success_criteria>

<output>
After completion, create `.planning/phases/19-d1-index-migration/19-01-SUMMARY.md`
</output>
