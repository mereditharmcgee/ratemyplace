-- Migration: Add missing columns that weren't in previous migration
-- NOTE: All columns below were already added in 0004_survey_scores.sql.
-- This migration is intentionally a no-op to preserve migration numbering.
SELECT 1;
